/**
 * TRUST & VERIFICATION METRICS CONFIGURATION
 * 
 * NOTE ON INTEGRITY:
 * Fabricated user testimonials, fake reviews, invented ratings, and fake organizational 
 * endorsements have been permanently removed from this directory. 
 * Only verifiable platform metrics (total categories, verified guide counts, gazette audit date) 
 * and real user submissions collected via verified forms should ever be displayed.
 */

export interface MediaMention {
  name: string;
  tagline: string;
  category: string;
}

export const TRUST_CONFIG = {
  activeYear: '2026',
  auditDate: 'August 2026',
  accuracyStandard: '100% Official Gazette Cross-Checked',
};

